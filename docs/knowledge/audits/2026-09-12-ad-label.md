# Gallery attribution spacing fix

The public ad gallery overlaid the attribution link on the creative. Its 24px minimum touch target covered text on small banners. The gallery now reserves a separate line below the creative, removes the white background and horizontal padding, and keeps the link's accessible target height. All original banner dimensions remain intact. The gallery canvas reserves the additional height before the renderer loads.

The change is scoped to .studio-ad. Publisher embeds and campaign-editor rendering are unchanged.

Source commit: 3668c0c. Production build and formatting passed. Browser geometry checks passed for all four formats, three languages and three widths (1440, 390, 320): no overlap, clipped label or undersized target. Inspected 300x250 and 320x50 screenshots. Dark-theme accessibility and both public Playwright journeys passed.

Deployed as frontend release 20260913-ad-label. The live browser repeated all 36 geometry checks and dark-theme accessibility with no issues. PM2 uses the new release with zero restarts.
